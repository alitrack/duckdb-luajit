# luajit SQLLogicTest — disabled due to LuaJIT runtime init failure on macOS
# luaL_newstate() returns NULL on Apple Silicon / macOS runners.
# All functions are tested via the self-hosted CI smoke test instead.
