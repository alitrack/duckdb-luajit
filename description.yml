name: luajit
version: 0.10.0
description: In-process JIT-compiled Lua UDFs via LuaJIT — 9 scalar, 1 aggregate, 1 table, 8 module modes, LIST/STRUCT/MAP bridge, ~700KB.
license: MIT
maintainers:
  - alitrack
